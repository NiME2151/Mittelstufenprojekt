package de.szut.msp_backend.events;

public class TalkToNPCGameAction implements GameAction
{
    /*
     * I am currently unsure if we ever need this here
     * */
    @Override
    public int doAction(final int clicks)
    {
        return 0;
    }
}
